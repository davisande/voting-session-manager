server.port: 8081
spring:
  jpa:
    hibernate.ddl-auto: none
  datasource:
    url: jdbc:postgresql://localhost:5432/voting-session-manager
    username: application
    password: application

lombok.nonNull.exceptionType: IllegalArgumentException